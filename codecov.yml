coverage:
  precision: 2
  round: down
  range: "60...80"
  status:
    project:
      default:
        target: 80

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false
