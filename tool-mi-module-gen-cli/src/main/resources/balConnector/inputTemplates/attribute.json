{
  "type": "attribute",
  "value": {
    "name": "{{name}}",
    "displayName": "{{displayName}}",
    "inputType": "{{inputType}}",
    "defaultValue": "{{defaultValue}}",
    "required": "{{required}}",
    "helpTip": "{{helpTip}}"{{#if validateType}},
    "validateType": "{{validateType}}"{{/if}}{{#if matchPattern}},
    "matchPattern": "{{matchPattern}}"{{/if}}{{#if isCombo}},
    "enableCondition": [
      {
      "forwardExistingHeaders": "{{enableCondition}}"
      }
    ]{{/if}}
  }
}
